---
"lalph": patch
---

Wire `lalph run feature <name>` into the real Ralph execution path for
stored Ralph-mode features, resolving their spec paths from feature metadata
and targeting the stored feature branch.
