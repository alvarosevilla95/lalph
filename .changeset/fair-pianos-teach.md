## "lalph": patch

Update standard PR-flow worker and reviewer prompts for `github-parent`
projects so they tell agents to read the bound project spec when `specPath` is
set, and add regression coverage for prompt generation with and without a spec.
