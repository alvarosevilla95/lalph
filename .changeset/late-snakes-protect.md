---
"lalph": patch
---

Add the `lalph run` command group with `run issues`, `run feature <name>`, and
`run all`, make bare `lalph` dispatch through `run all`, and cover the command
surface with dispatch-focused CLI tests.
