## "lalph": patch

Add `github-parent` project configuration for PR-flow GitHub projects, validate
bound parent issues during add/edit, derive parent target branches when
appropriate, surface the new selection mode in project inspection output, and
discover runnable work from the bound parent issue's open direct child issues.
