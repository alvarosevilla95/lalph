---
"lalph": patch
---

Derive feature display status for `lalph features ls` and
`lalph features show` from final PR state plus execution progress instead of
echoing persisted lifecycle metadata directly.
